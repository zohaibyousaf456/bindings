// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  assetPrefix: 'https://cdn.jsdelivr.net/gh/zohaibyousaf456/my-static-next@v1.0.1',
};

export default nextConfig;
